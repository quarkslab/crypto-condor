"""Module for cryptographic primitives."""

import logging

# Sets default level to WARNING, still requires someone to call BasicConfig.
logger = logging.getLogger("crypto-condor")
logger.setLevel(logging.WARNING)
